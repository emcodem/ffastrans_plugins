Develper instructions:
If you dont have nodejs installed yet, download and install it, then install pkg using Command:
npm i pkg

ready to use binaries were built with nodejs pkg command, this should write the built binaries directly into this directory. Command:
pkg ffastrans_remote.js

